describe('MapViewBar', () => {
  it.skip('should call changeDisplayMode on tool buttons click', () => {
    /* const setTwoDDisplayMode: (mode: T.TwoDDisplayMode) => void = (mode) => {
      renderResult.rerender(
        <Provider store={store}>
          <ContentPage {...props} twoDDisplayMode={mode} />
        </Provider>,
      );
    };
    setTwoDDisplayMode(T.TwoDDisplayMode.NORMAL);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[0]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.COMPARISON2);

    jest.resetAllMocks();

    setTwoDDisplayMode(T.TwoDDisplayMode.COMPARISON2);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[0]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.NORMAL);

    jest.resetAllMocks();

    setTwoDDisplayMode(T.TwoDDisplayMode.NORMAL);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[1]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.COMPARISON4);

    jest.resetAllMocks();

    setTwoDDisplayMode(T.TwoDDisplayMode.COMPARISON4);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[0]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.NORMAL);

    jest.resetAllMocks();

    setTwoDDisplayMode(T.TwoDDisplayMode.NORMAL);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[2]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.SLIDER);

    jest.resetAllMocks();

    setTwoDDisplayMode(T.TwoDDisplayMode.SLIDER);
    ReactTesting.fireEvent.click(renderResult.queryAllByTestId('tool-button')[2]);
    expect(props.changeDisplayMode).toHaveBeenCalledTimes(1);
    expect(props.changeDisplayMode).toHaveBeenLastCalledWith(T.TwoDDisplayMode.NORMAL);

    jest.resetAllMocks(); */
  });
});
