import { storiesOf } from '@storybook/react';
import React from 'react';

import FrontFooter from './';

storiesOf('Molecules|FrontFooter', module)
  .add('default', () => (
    <FrontFooter />
  ));

