describe('MapController', () => {
  it.skip('should call map controllers', () => {
    /* renderResult.rerender(
      <Provider store={store}>
        <ContentPage {...props} mapCenter={[0, 0]} />
      </Provider>,
    );
    expect(props.onMapCenter).toHaveBeenCalledTimes(0);
    expect(props.changeZoom).toHaveBeenCalledTimes(0);

    ReactTesting.fireEvent.click(renderResult.getByTestId('map-center-btn'));
    expect(props.onMapCenter).toHaveBeenCalledTimes(1); */
  });
});
