/**
 * @todo Please write zindex value at here
 * Please use enum by component
 */
/* eslint-disable no-magic-numbers */

export enum Global {
  TUTORIAL = 99999999,
  GUIDE = 99999998,
  ATTACH_DROPDOWN = 50,
}

export enum SideBar {
  SUB_CALENDAR = 50,
  SOURCE_UPLOAD = 50,
}

export enum ContentListItem {
  DEFAULT = 140,
}

export enum OutsideMap {
  VOLUME_ALGORITHM = 1,
  SBVC_DBVC_POPUP = 5,
}

export enum ContentPagePopup {
  DEFAULT = 300,
  BLUEPRINT_ALIGN_POPUP = 100,
}

export enum MapController {
  DEFAULT = 230,
}

export enum CesiumViewer {
  LOADING_SCREEN = 1,
}

export enum LayerZIndex {
  LENGTH_MOVABLE_POINT = 999999999999999,
}

export enum SharePage {
  HEADER = 1000,
}

export const enum LayerGroupZIndex {
  MAP = 1,
  DSM,
  SLIDER_MAP,
  BLUEPRINT,
  LENGTH,
  AREA,
  DESIGN_DXF,
  DESIGN_DXF_ERROR_BOUNDARY,
  DESIGN_DXF_BOUNDARY,
  VOLUME_SURVEY_DSM,
  VOLUME,
  PRINT,
  GEOLOCATION_ACCURACY,
  GEOLOCATION_POSITION,
}
